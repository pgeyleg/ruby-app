version: '3'
services:
  web:
    image: web:dev
    command: ./bin/docker-dev-server.sh
    build:
      context: .
    environment:
      RAILS_ENV: development
      RAILS_PORT: 3010
    volumes:
      - bundle_cache:/bundle
      - .:/app
    ports:
      - 3010:3010

volumes:
  bundle_cache:
